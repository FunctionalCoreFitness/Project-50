{
  "name": "project50-healthkit-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Ingest + read API for Project 50 HealthKit sync (single-user).",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.9.0",
    "express": "^4.19.2"
  }
}
